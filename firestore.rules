rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Todo acceso pasa por las Cloud Functions (Admin SDK, ignora estas reglas).
    // Sin acceso directo desde cliente.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
