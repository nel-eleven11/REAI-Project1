{
  "name": "functions",
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && cp ../plan.md ../public/plan.md && firebase emulators:start --only functions,firestore,hosting",
    "shell": "npm run build && firebase functions:shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "test": "node --test",
    "test:emulator": "npm run build && firebase emulators:exec --project demo-test --only functions,firestore \"npm test\""
  },
  "dependencies": {
    "express": "^4.21.0",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.3.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "typescript": "^5.6.0"
  },
  "private": true
}
